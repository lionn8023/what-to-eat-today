# 高德地图 Web 端（JS API）Key
# 申请地址：https://console.amap.com/dev/key/app （创建「Web端(JS API)」类型应用）
VITE_AMAP_KEY=你的高德Key

# 高德地图安全密钥（securityJsCode），与上面的 Key 配套下发
VITE_AMAP_SCODE=你的安全密钥

# Supabase（数据库 / 登录 / 收藏 / 抽签历史）配置
# 申请地址：https://supabase.com → 新建项目 → Project Settings → API
# 拿到 Project URL 与 anon public key 填到下面（VITE_ 前缀，会进入前端但属「受控公开」，权限由数据库 RLS 控制）
VITE_SUPABASE_URL=你的Supabase项目URL
VITE_SUPABASE_ANON_KEY=你的Supabase_anon_public_key

# 以下为服务端保活函数使用（在 Vercel 环境变量里配置，不要带 VITE_ 前缀，也不要写进前端）
SUPABASE_URL=你的Supabase项目URL
SUPABASE_ANON_KEY=你的Supabase_anon_public_key
