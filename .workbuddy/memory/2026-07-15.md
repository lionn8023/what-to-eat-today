# 2026-07-15

## 接入 Vercel Web Analytics
- 安装 `@vercel/analytics`，在 `src/main.jsx` 顶层挂载 `<Analytics />` 组件（默认仅 production 环境上报，本地 dev 不污染数据）。
- `npm run build` 通过（49 modules）。
- 同日追加 `@vercel/speed-insights`，main.jsx 同时挂 `<Analytics />` 与 `<SpeedInsights />`（均默认仅 production 上报）。build 通过（50 modules）。
- 已 commit `142533a` 并 push 到 GitHub（master），Vercel 会基于仓库自动重新部署。
- 待用户在 Vercel Dashboard 开启：Analytics（Web Analytics）+ Speed Insights（Core Web Vitals），之后即可看访问量/访客/来源与性能数据。
