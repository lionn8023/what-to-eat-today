# 2026-07-21 工作日志

## 规划：接入 Supabase 数据库
用户想把「今天吃什么」项目接入 Supabase，扩展数据库功能。本轮为方案规划（未动手写代码），已确认方向：
- Supabase Free 层够用（2 项目 / 500MB DB / 5GB 带宽 / 5万 MAU），**但免费项目 7 天无活动会被暂停（pause）**——作品集需注意（定时 ping 或升 Pro $25/月）。
- 候选功能：① 收藏/想吃清单 ② 抽签历史时间线 ③ 账户登录(跨设备) ④ 店铺评分/笔记 ⑤ 多人投票转盘(进阶) ⑥ 公开吃啥墙(社区)。
- 安全：前端用 anon key（设计上公开，靠 RLS 控权），service_role key 绝不能进前端；建表后必开 RLS + 写 policy。
- 待用户确认第一版功能范围后动手；建 Supabase 项目需用户自己账号（拿 URL + anon key）。
